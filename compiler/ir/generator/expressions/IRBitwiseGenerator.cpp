#include "IRBitwiseGenerator.h"
#include "../IRGenerator.h"

string IRBitwiseGenerator::genAnd(AndBitwise *b_and)
{

	// string var1 = generator->genExpr(b_and->getExpr1());
	// string var2 = generator->genExpr(b_and->getExpr2());
	// string var3 = generator->createTempVar(INT_32);

	// vector<string> params;
	// params.push_back(var3);
	// params.push_back(var1);
	// params.push_back(var2);

	// generator->getCurrentBB()->add_IRInstr(IRInstr::B_AND, "int32", params);

	// return var3;
	return "";
}

string IRBitwiseGenerator::genOr(OrBitwise *b_or)
{

	// string var1 = generator->genExpr(b_or->getExpr1());
	// string var2 = generator->genExpr(b_or->getExpr2());
	// string var3 = generator->createTempVar(INT_32);

	// vector<string> params;
	// params.push_back(var3);
	// params.push_back(var1);
	// params.push_back(var2);

	// generator->getCurrentBB()->add_IRInstr(IRInstr::B_OR, "int32", params);

	// return var3;
	return "";
}

string IRBitwiseGenerator::genXor(XorBitwise *b_xor)
{

	// string var1 = generator->genExpr(b_xor->getExpr1());
	// string var2 = generator->genExpr(b_xor->getExpr2());
	// string var3 = generator->createTempVar(INT_32);

	// vector<string> params;
	// params.push_back(var3);
	// params.push_back(var1);
	// params.push_back(var2);

	// generator->getCurrentBB()->add_IRInstr(IRInstr::B_XOR, "int32", params);

	// return var3;
	return "";
}
